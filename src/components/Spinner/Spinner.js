import SpinnerWrapper, { Loader } from "./Spinner.style";

const Spinner = () => {
    return (
        <SpinnerWrapper>
            <Loader />
        </SpinnerWrapper>
    );
};

export default Spinner;
